<div class="container-fluid">

  <div class="text-center">
    <div class="header">
      <h2><%= @theme.name %></h2>
    </div>
  </div>

  <div class="row">
    <div class="text-center primary" style="margin-top:2em;">
        <%= button_to "Get New Article and Tip", refresh_path(@theme), method: :post, class: "btn btn-success", remote: true %>
    </div>
  </div>

  <%= render partial: "themes/tipsContent", locals: {tips: @tips, content: @content} %>

</div>
